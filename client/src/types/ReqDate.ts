export interface RequestDate {
    startDate: string,
    endDate: string,
    description: string,
    price: number,
}

export interface ResponseDate {
    _id: string,
    startDate: string,
    endDate: string,
    description: string,
    price: number,
}
